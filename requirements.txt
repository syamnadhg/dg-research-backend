# Multi-Agent Deep Research Pipeline
#
# ⛔ THIS FILE AND pyproject.toml ARE ONE FACT IN TWO PLACES. Everything above
# the "Testing" block mirrors pyproject's `[project] dependencies` EXACTLY, in
# both directions; the Testing block mirrors its `[dev]` extra. A source
# checkout installs from THIS file and nothing else, while a published wheel
# resolves from pyproject alone — so a dep added to one and not the other is an
# ImportError that appears only on the side the author was not looking at.
#
# Version floors bumped 2026-06-30 to the versions we actually run + test
# against (see pyproject.toml — kept in sync). `>=` still lets a fresh install
# pull anything newer.
# Core
anthropic>=0.85
playwright>=1.61
# patchright is a stealth drop-in for playwright's async API — the only
# browser import the pipeline makes (`from patchright.async_api import
# async_playwright`, in `Browser.start`) since the 2026-04-19 stealth
# migration. Without this, every fresh `--pair` install crashes after
# stage 2 with ModuleNotFoundError.
#
# Floor bumped 2026-06-30 to the current line (patchright 1.61.1 /
# playwright 1.61.0). patchright's whole value is up-to-date stealth
# patches that track bot-detection; running ~3 releases behind is exactly
# when Cloudflare's Turnstile starts looping. Upgrade an existing source
# checkout with:  pip install -U patchright playwright && patchright install chrome
#
# ⛔ NAME THE SYMBOL, NOT A LINE NUMBER. This comment used to point at
# "start_browser_session at research.py:8186". That function no longer exists
# and the line now lands in unrelated Firestore-outage code, so the one
# pointer a reader had led somewhere wrong. A file this long renumbers on
# every commit; `Browser.start` does not.
patchright>=1.61

# Extraction
markdownify>=1.2
requests>=2.32
# ⛔ DIRECT, not incidental — research.py imports urllib3.connection /
# urllib3.connectionpool and subclasses their internals for the document-image
# fetch and upload. It arrived transitively via requests until 2026-09-18; the
# floor is 2.x because that is the shape the subclassing assumes.
urllib3>=2.0
# Phase-2 source-doc inlining for Gemini (paste path): when P1 is skipped
# and the user attaches .pdf/.docx sources, Gemini gets their text pasted
# (it silently drops file uploads). md/txt need no lib; these cover the
# rest. Both imports are guarded in _extract_source_text — a missing lib
# degrades to "source not inlined" (WARN) rather than crashing, so an
# un-upgraded venv still runs.
pypdf>=4.0
python-docx>=1.2

# Image/video
Pillow>=11.0

# Server (for web app)
fastapi>=0.136
uvicorn[standard]>=0.46
websockets>=14.0
pydantic>=2.11

# Firestore client — BE authenticates as a per-user synthetic device
# user via refresh-token credentials from the OS keystore (no Admin
# SDK, no service-account JSON). `Client(credentials=...)` is stable
# from 2.20+.
google-cloud-firestore>=2.27
# Carries the `Credentials` base class that auth/credentials.py's
# RefreshTokenCredentials subclasses.
google-auth>=2.50
# Cross-platform OS keystore for the refresh-token slot rotation in
# auth/keystore.py. Falls back to a chmod-0600 file under
# ~/.super-research/ on headless Linux without secret-service.
keyring>=25.7

# Process hygiene — orphan-Chrome cleanup after Stop/respawn, sibling
# dual-spawn detection (worker-lock PID-liveness), and the unpair process
# sweep. Every `import psutil` is try/except-guarded (a missing psutil
# degrades those features rather than crashing), but it IS a real runtime
# dep, so declare it — a fresh install should get full process hygiene.
psutil>=6.0

# Setup QR code rendering
qrcode>=8.0

# Podcast duration probe — pure Python (MIT), reads m4a/mp3/wav metadata
# with no system binary. 2026-07-12: replaces the hard ffprobe dependency
# (_audio_duration_sec: tinytag primary, ffprobe guarded fallback) so a
# fresh install probes duration correctly with no ffmpeg on the machine.
# Keep in sync with pyproject.toml.
tinytag>=2.0

# Testing — added 2026-05-07 to support F4 security tests (DGOPS-7451).
# tests/test_f4_security.py runs pure-unit + mocked-context tests for
# _matches_security_deny_host, _read_security_deny_hosts,
# _detect_persisted_google_auth, _scrub_persisted_google_auth.
# Run via `pytest tests/test_f4_security.py -v`.
#
# These three are pyproject.toml's `[dev]` extra — see the header.
pytest>=8.0
pytest-asyncio>=0.23
# ⛔ PINNED EXACTLY, not floored — the same reason pyproject.toml pins it
# (DGOPS-9508): rule implementations change between ruff releases, so a floor
# reproduces the ruleset but not the count, and CI and a developer's machine
# stop agreeing about a clean tree. Was missing here while pyproject carried
# it, so `pip install -r requirements.txt` left the machine without ruff and
# tests/test_lint_correctness_floor.py silently SKIPPED its only real test.
# The CI workflow still installs it in its own pinned step; the two agree.
ruff==0.16.0
