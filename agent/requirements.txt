# Super Agent bridge — runtime dependencies (Python 3.11+).
#
# NORMAL PATH: you do NOT need this file. Run the agent as a backend command —
#     python research.py agent <command>
# — its two deps already ship in the backend's ../requirements.txt.
#
# This file is only for running the agent STANDALONE in a fresh venv (without
# the backend installed), e.g.:
#     pip install -r requirements.txt   &&   python -m facade <command>
#
# pyproject.toml is the canonical dependency source; this mirrors its runtime deps.

requests>=2.31
keyring>=24
